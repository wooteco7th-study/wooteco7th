package subway.support;

// 단순 초기화 기능만 수행
// 저장소 역할 X
public class Initializer {

//    public Sections initializeSections() {
//        return new Sections(SectionType.findAll().stream()
//                .map(this::makeSection)
//                .toList());
//    }
//
//    private Section makeSection(final SectionType section) {
//        return new Section(StationType.valueOf(section.getDepartureStationName()),
//                StationType.valueOf(section.getArrivalStationName()), section.getTakenTime(),
//                section.getDistance());
//    }
}
